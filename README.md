# Iris-Classifier ( Decision Tree)

## Overview
End-to-end ML example from Digital Marketing Mastery Module->builds a decision tree classifier
on the classic iris dataset using scikit-learn.

##Quick start

export PAT=<Personal Access Token >  # must obtain it from the remote repository
ksh
git clone https:/${PAT}@github.com/azaman-github/iris-classifier.git
cd iris-classifier
python src/train.py
